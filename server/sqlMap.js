// sql”Ôæ‰
var sqlMap = {
    // ”√ªß
    channel:{
        list:'select * from channel'
        //num:'select a.id,a.title,a.type,a.author,a.created_at,a.src_url,b.channel_id from object_detail a,channel_object_rel b where a.id=b.object_id,channel_id=?'
    }
}
module.exports = sqlMap;
